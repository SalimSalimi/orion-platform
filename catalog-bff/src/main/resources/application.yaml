server:
  servlet:
    context-path: /api/bff
  port: 8081

app:
  oidc:
    base-url: http://localhost:8085/realms/catalog-realm

spring:
  application:
    name: catalog-bff

  security:
    oauth2:
      client:
        registration:
          keycloak:
            client-id: catalog-bff
            client-secret: Wf3pvZcrNuOlGnsTbheGJdC9laww16fr
            scope: openid
        provider:
          keycloak:
            issuer-uri: ${app.oidc.base-url}

logging:
  level:
    org.springframework.web.client.RestTemplate: DEBUG